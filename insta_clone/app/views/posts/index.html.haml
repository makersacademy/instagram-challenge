- if @posts.any?
  - @posts.each do |post|
    = link_to (image_tag post.image.url(:medium), class:'img-responsive'), post_path(post)
    %h2= post.caption
- else
  No posts yet
%a(href='/posts/new') Post to instagram
