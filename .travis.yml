rvm: '2.4.0'

script:
  - mkdir -p spec && bundle exec rspec spec

before_install:
- sudo apt-get update -qq
- sudo apt-get install -qq postgresql-server-dev-9.3
- export TZ=Europe/London

before_script:
  - psql -c 'create database impostagram_test;' -U postgres
