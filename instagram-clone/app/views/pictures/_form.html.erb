<%= form_for @picture, html: {multipart: true} do |f| %>
  <%= f.file_field :image %>
  <%= f.text_field :name, placeholder: "Name your picture..." %>
  <%= f.text_area :description, placeholder: "Describe your picture..." %>
  <%= f.submit "Post" %>
<% end %>
