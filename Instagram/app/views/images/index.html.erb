<a href='/images/new'><button>Add an image</button></a>

<% for image in @images do %>
  <p><%= image.user %></p>
  <%= link_to(image_tag(image.image.url(:med)), image_path(image)) %>
  <p><%= image.caption %></p>
<% end %>
