<%= form_with(model: [ @post, @post.likes.build ], local: true) do |form| %>
  <p>
    <%= form.label :like %><br>
    <%= form.select :number, (1...2) %>
  </p>
  <%= form.hidden_field :user_id, :value => current_user.id %>
  <p>
    <%= form.submit :Like%>
  </p>
<% end %>
