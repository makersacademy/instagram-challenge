$("#post-<%= @post.id %>-like-button").html("<%= escape_javascript(render partial: 'posts/like_button', locals: { post: @post }) %>");
$("#post-<%= @post.id %>-like-count").html("<%= escape_javascript( @post.likes.count.to_s ) %>");