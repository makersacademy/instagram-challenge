<% if @photographs.any? %>
 <% @photographs.each do |photograph| %>
    <%= photograph.description  %>
    <%= link_to "Delete post", photograph_path(photograph), method: :delete %>
    <% end %>
<% else %>
  <h1>No posts yet</h1>
<% end %>



<a href="photographs/new">Add a post</a>
