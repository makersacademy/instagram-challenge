<section class="feed">
<h1> Show me the pans </h1>
<% for entry in @entries %>
<div class='post'>
  <img src=<%= entry.image %>>
  <p><%= entry.description %></p>
  <p class='edit'><%= link_to('Edit', edit_entry_path(entry)) %></p>
</div>
<% end %>
</section>