<section>
  <section class="feed-header">
    <h4>#<%= @tag.text %></h4>
  </section>
  <section class="photo-grid">
    <% @tag.photos.reverse.each do |photo| %>
      <section class="single-photo">
        <%= link_to photo_path(photo) do %>
          <%= image_tag photo.image.url(photo.return_filter) %>
        <% end %>
      </section>
    <% end %>
  </section>
</section>
