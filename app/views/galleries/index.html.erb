
<%if @galleries.any? %>
  <%@galleries.each do |gallery| %>
    <h2><%= gallery.name %>
  <% end %>
<% else %>
  <h2>No galleries yet</h2>
<% end %>

<%= link_to("Add a gallery", new_gallery_path) %>
