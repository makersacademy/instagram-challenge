%div
- if @pun.errors.any?
  %section#errors
    %h2
      = pluralize @pun.errors.count, "error"
      prohibited this pun from being saved:
    %ul
      - @pun.errors.full_messages.each do |message|
        %li= message

.bg-primary
  = form_for @pun, html: {multipart: true} do |f|
    %h4= f.label :name
    %h4= f.text_field :name
    %h4= f.label :description
    %h4= f.text_area :description
    %h4= f.file_field :image
    %h4= f.submit
