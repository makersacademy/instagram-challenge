<!DOCTYPE html>
<title> Metalshot </title>
<h4>Welcome to Metalshot \m/</h4>
  The time is now: <%= Time.now %>

<% if current_user %>
<h3>☠ Your shots:</h3>
<% current_user.images.each do |image| %>
<img src="<%= formatted_image_url(image) %>" />
<% end %>
<% end %>
