<div class="container">
  <div class="row d-flex align-items-center justify-content-center">
    <div class="col-md-6">
      <% if current_user %>
        <%= link_to '+ New Post', new_post_path, class:"btn btn-primary w-100 my-3 link-light text-decoration-none" %>
      <% else %>
        <%= link_to '+ New Post', new_user_path, class:"btn btn-primary w-100 my-3 link-light text-decoration-none" %>
      <% end %>
    </div>
  </div>
</div>

<%= render 'posts' %>