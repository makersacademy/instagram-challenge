<div class="pic-list">
  <% @pics.each do |pic| %>
  <div class="pic-card">
    <%= link_to image_tag(pic.picture.url, class: "pic-card__image"), pic %>
    <h4><%= pic.title %></h4>
  </div>
  <% end %>
</div>
