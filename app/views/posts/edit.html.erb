<%= form_for @post do |form| %>
  <%= image_tag(@post.image) %><br>
  <%= form.text_field :caption, name: "caption", value: @post.caption %><br>
  <%= form.file_field :image, name: "image", class: "upload-btn" %><br>
  <%= form.submit "Save" %>
<% end %>

<%= notice %>
