%a(href=new_post_path) Post a photo

- if @posts.any?
  %ul
    - @posts.each do |post|
      %li
        = post.user.email
        = image_tag post.image.url(:medium)
        = post.caption
- else
  No posts, upload one!
