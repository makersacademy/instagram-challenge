- if user_signed_in?
  = form_for Post.new do |f|
    = f.label :title
    = f.text_field :title
    = f.label :body
    = f.text_area :body, size: '60x12'
    = f.submit 'Post'

- @posts.each do |post|
  = post.title
  = post.body