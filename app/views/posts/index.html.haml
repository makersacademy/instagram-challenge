!!!
%html
  %body
    - if @posts.any?
      - @posts.each do |post|
        = image_tag post.image.url(:thumb)
        %h2= post.caption
    - else
      No posts yet

    = link_to 'Add a post', new_post_path
