- if @posts.any?
  - @posts.each do |post|
    =image_tag post.image.url(:regular)
    %h2= post.title
    = link_to 'Edit', edit_post_path(post)
- else
  %h1 No posts yet
%br
%a(href='/posts/new') Add a post
