
.posts-wrapper.row
  -@posts.each do |post|
    .post
      .post-head
        .name
          Post's owner -# NOTE: impelement logic
      .image.center-block
        =link_to (image_tag post.image.url(:medium), class: 'img-responsive'), post_path(post)
      %p.caption
        =post.caption
