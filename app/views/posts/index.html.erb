<h1>Pictagram</h1>

<%= link_to 'New Post', new_post_path %>

<% @posts.each do |post| %>
  <%= link_to (image_tag post.image.url(:web, :preview)), post_path(post) %>
  <p><%= post.text %></p>
<% end %>