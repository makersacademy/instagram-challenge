<h1>Posts index</h1>
<p>
  <%= link_to 'create post', new_post_path %>
</p>
<ul>
  <% @posts.each { |post| %>
    <h2><%= post.title %></h2>
    <p>
      <%= post.description %>
    </p>
    <p>
      <%= link_to 'Show', post_path(post) %>
    </p>
  <% } %>
</ul>