<h1> Hipstagram </h1>

<% @posts.each do |post| %>
  <%= image_tag(post.image.url, alt: 'Image') if post.image? %>
  <h3><%= post.caption %></h3>
<% end %>





<%= link_to 'Add post', new_post_path %>
