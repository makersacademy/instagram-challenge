<%= link_to "New Post", new_post_path %>
<br>
<% if @posts.any? %>
  <% @posts.each do |post| %>
    <%= image_tag post.image.url(:medium) %>
    <%= post.caption %>
  <% end %>
<% else %>
    No posts yet!
<% end %>
