<% if @posts.any? %>
  <% @posts.each do |post| %>
    <h2><%= post.title %></h2>
    <h3><%= post.description %></h3>
    <%= image_tag post.image.url(:medium) %>
  <% end %>
<% else %>
  <h1>No posts yet</h1>
<% end %>

<a href="/posts/new">Create a Post</a>
