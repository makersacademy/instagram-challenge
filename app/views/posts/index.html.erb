<h1>Influenstagram</h1>

<% @posts.each do |post| %>
  <div class="image_post">
    <img src="<%= post.image_url %>" />
    <%= button_to 'Delete', post_path(post), method: :delete %>
  </div>
<% end %>