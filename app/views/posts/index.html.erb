<% if @posts.any? %>
  <% @posts.each do |post| %>
    <br>
    <%= post.title %>
    <%= image_tag post.image.url(:medium) %>
    <br>
  <% end %>
<% else %>
  No posts yet!
<% end %>

<br>
<%= link_to 'Add a post', new_post_path %>