<% if @posts.any? %>
  <% @posts.each do |post| %>
    <h2> <%= link_to post.content, post_path(post) %> </h2>
  <% end %>
<% else %>
  No posts yet
<% end %>

<%= link_to "Add a post", new_post_path %>
