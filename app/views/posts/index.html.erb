
<% if @posts.any? %>
  <% @posts.each do |post| %>
    <%= image_tag post.image.url(:thumb) %>
    <h2> <%= link_to post.title, post_path(post) %> </h2>
  <% end %>
<% else %>
  No photos yet!
<% end %>

<a href='/posts/new'>Add a photo</a>

