<% if Post.all.any? %>
  <% Post.all.each do |post| %>
    <h2><%= post.title %></h2>
    <%= image_tag post.image.url(:thumb) %>
  <%end%>
  <%else%>
    <h1>No posts yet</h1>
<%end%>

<a href='/posts/new'>Add a post</a>'
