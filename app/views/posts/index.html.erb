<% if @posts.any? %>
  <% @posts.each do |post| %>
      <ul>
        <li>
          <%= link_to image_tag(post.image.url(:med)), post.image.url %>
          <%= post.caption %>
        </li>
      </ul>
  <% end %>
    <% else %>
      <p>No posts.</p>
<% end %>