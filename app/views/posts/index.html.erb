<h2>Latest Posts</h2>

<div class="postsWrapper">

  <% @posts.each do | post | %>
    <div class="post"> 
      <div class="author-bar">
        <a href="user_posts/<%= post.user.id %>"><i class="fas fa-user-circle"></i><p id="handle"><%= post.user.handle %></p></a>
      </div>
      <img src="<%= post.image %>">
      <p><%= post.description %></p>
    </div>
  <% end %>

</div>
