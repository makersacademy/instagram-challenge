<h1>Instagram</h1>
<%= link_to "New Post", new_post_path %>

<% @posts.each do |post| %>
  <li>
    <div><%= image_tag  post.image.url(:medium) %></div>
    <div><%= post.caption %></div>
  </li>
<% end %>