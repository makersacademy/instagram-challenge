<% if @posts.any? %>
  <% @posts.each do |post| %>
      <ul>
        <li>
          <%= link_to image_tag(post.image_file_name), post.image_file_name %>
          <%= post.caption %>
        </li>
      </ul>
  <% end %>
    <% else %>
      <p>No posts.</p>
<% end %>