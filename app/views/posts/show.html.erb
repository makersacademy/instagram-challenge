<p id="notice"><%= notice %></p>

<p>
  <strong>Entry:</strong>
  <%= @post.entry %>
  <% if @post.avatar.attached? %>
    <td><%= image_tag @post.avatar.representation(resize_to_limit: [nil, 50]) %></td>
  <% end %>
</p>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
