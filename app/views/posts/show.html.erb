<div class="card">
  <div class="card-header bg-dark text-light">@<%= @post.user.username %>
    <span class="float-end">
      <%= link_to 'Edit', edit_post_path(@post), class:"btn btn-sm btn-outline-secondary" %> 
      <%= link_to 'Delete', @post, method: :delete, data: { confirm: 'Are you sure?' }, class:"btn btn-sm btn-outline-secondary" %>
    </span>
  </div>
  <div class="card-body">
    <p><%= image_tag @post.image.variant(resize_to_limit: [800, 800]) %></p>
    <p class="card-text"><%= @post.title %></p>
  </div>
</div>


<br/>
<div class="card">
  <div class="card-header"> <%# ROUTE TO BE CHANGE  %>
    <h5>Comments<span class="float-end"><%= link_to 'Add', new_comment_path, class:"btn btn-sm btn-dark" %> </span></h5>
  </div>
  <%# <% @comments.each do |comment| %> %>
    <%# <div class="card-body"> %>
      <%# <p class="card-title text-dark">@<%= comment.user.username %></p> %>
      <%# <p class="card-text"><%= comment.body %></p> %>
    <%# </div> %>
  <%# <% end %> %>


</div>

<br/>

<%= link_to 'Back', posts_path, class:"btn btn-sm btn-dark" %>

