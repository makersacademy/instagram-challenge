= build_filtered_image_for(@post)
= link_to 'Add a comment', new_post_comment_path(@post)

- if @post.comments.any?
  %ul
  - @post.comments.each do |comment|
    %li= comment.body
- else
  %em No comments