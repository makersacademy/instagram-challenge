%h2= @post.title
= image_tag @post.image.url(:thumb)
%p= link_to "Delete #{@post.title}", post_path(@post), method: :delete
%p= link_to "Comment on #{@post.title}", new_post_comment_url(@post)
- @post.comments.each do |comment|
    %p= comment.comment


/ <!-- <!-- <h2> <%= @post.title %> </h2>
/   <%= image_tag @post.image.url %>
/   <p> <%= link_to "Delete #{@post.title}", post_path(@post), method: :delete %> </p>
/     <p> <%= link_to "Comment on #{@post.title}", new_post_comment_url(@post) %> </p>

/ <% if @posts.comments.any? %>
/   <% @posts.comments.each do |comment| %>
/     <p> <%= comment.comment %> </p>
/ <% end %>
/ <% end %>
/  -->