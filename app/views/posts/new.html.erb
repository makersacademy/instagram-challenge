New post

<%= form_with model: @post do |form| %>
  
  <div>
    <%= form.text_area :body %>

  <% @post.errors.full_messages_for(:body).each do |message| %>
  <div><%= message %></div>
    <% end %>
  </div>

  <div>
    <%= form.submit "Post" %>
  </div>
<% end %>