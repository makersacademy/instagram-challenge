<%= form_for @post, html: {multipart: true} do |f| %>
  <%= f.label :description %>
  <%= f.text_area :description %>
  <%= f.file_field :image %>
  <%= f.select :filter, ['none', 'greyscale', 'huerotate', 'invert'] %>
  <%= f.submit %>
<% end %>
<div id="target">
</div>
