<h2>New Post</h2>
<hr>

<%= form_for @post do |f| %>
  <%= f.file_field :image %>
  
  <br>
  <br>
<%= f.text_area :description %>
  
  <br>
<%= image_tag(@post.image) if @post.image.attached? %>
  <br>
 
<%= f.submit %>
<% end %>

