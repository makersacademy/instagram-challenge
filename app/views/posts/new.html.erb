<%= simple_form_for @post, url:posts_path do |form| %>
  <%= form.input :body, as: :text, label: 'Caption your moment' %>
  <%= form.input :image, as: :file %>
  <%= form.button :submit, class:'button' %>
<% end %>
