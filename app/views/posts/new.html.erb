<%= form_for @post do |form| %>

  <%= form.label :image %>
  <%= form.file_field :image %><br>

  <%= form.label :caption %>
  <%= form.text_field :caption, :maxlength => 500, :size => 40 %><br>

  <%= form.submit "Submit" %>
<% end %>
