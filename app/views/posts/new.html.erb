<%= form_for @post, html: {multipart: true} do |f|%>
    <%= f.label :title %>
    <%= f.text_field :title %>
    <%= f.file_field :image %>
    <%= f.submit "Post" %>
<% end %>

<% if @user.id == @post.user_id %>
    <%= link_to edit_post_path %>
<% end %>
