<% if @posts.any? %>
  <% @posts.reverse.each do |post| %>
    <h2> <%= link_to post.caption, post_path(post) %> </h2>
  <% end %>
<% else %>
  <p> No posts yet </p>
<% end %>

<%= link_to 'Add a post', new_post_path %>
