<html>
<head>
  <meta charset="utf-8">
  <title>Going post'all</title>
</head>
<body>
  <h1>Gonna post y'all</h1>
  <% if @posts.all.any? %>
  <% @posts.all.each do |post|  %>
  <div class="desc">
    <%= link_to post.caption, post_path(post) %>
    <h2><%= post.description %></h2>
    <% post.comments.each do |comment| %>
    <h2>Comments: <%= comment.thoughts %></h2>
    <% end %>
  </div>
  <div class="img">
    <%= image_tag post.image.url %>
  </div>
  <div class="desc">
    <span><%= link_to "Like post", new_post_like_path(post) %></span>
    <span><%= pluralize post.likes.count, 'like' %></span>
    <ul>
      <li><p><%= link_to "Edit #{post.caption}", edit_post_path(post) %></p></li>
      <li><p><%= link_to "Review #{post.caption}", new_post_comment_path(post) %></p></li>
      <li><p><%= link_to "Delete #{post.caption}", post_path(post), method: :delete %></p></li>
    </ul>
  </div>
  <% end %>
  <% else %>
  <h1>No posts yet!</h1>
  <% end %>
  <div class="post">
    <a href="/posts/new">Add a post</a>
  </div>
</body>
</html>

<style>

body {
  background-image: url('http://www.easterlingstudios.com/assets/images/background.jpg');
  background-position: center;
}

div.img img {
  width: 30%;
  height: auto;
  display: block;
  border: 8px solid white;
  margin-left: auto;
  margin-right: auto;
}

div.desc {
  padding: 15px, 15px;
  text-align: center;
  font-family: 'Arima Madurai', cursive;
  color: white;
  word-wrap: normal;
}

h1 {
  text-align: center;
  font-family: 'Arima Madurai', cursive;
  color: white;
}

h2 {
  text-align: center;
  font-family: 'Arima Madurai', cursive;
  color: #43200D;
}

a {
  font-family: 'Arima Madurai', cursive;
  font-size: 1.5em;
  color: white;
}

a:hover {
  color: #75EBFA;
}

ul {
  list-style-type: none;
  margin: 0;
  overflow: hidden;

}

li {
  float: left;
}

li a {
  display: block;
  color: white;
  text-align: center;
  margin-left: 150;
  text-decoration: none;
}

div.post {
  text-align: right;
  font-family: 'Arima Madurai', cursive;
  font-size: 1em;
  color: white;
  position: fixed;
  top:10;
  right:25;
}

</style>
