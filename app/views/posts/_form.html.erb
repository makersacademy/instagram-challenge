<!-- send files to the server: -->
<%= form_for @post, :html => {:multipart => true} do |f| %>
<!-- adds file picker so users can select an iamge from their local machine and upload it -->
  <%= f.file_field :image %>
  <%= f.text_field :name %>
  <%= f.submit 'Add post' %>
<% end %>
