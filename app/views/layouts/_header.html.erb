  <nav>
    <a href = "/">
      <img src= '/new-instagram-text-logo.png' class = 'nav-logo'>
    </a>
    <ul>
      <div>
        <% if user_signed_in? %>
        <li>
          Welcome, <strong><%= current_user.first_name %></strong>.</li>
          <%= link_to "Log Out", destroy_user_session_path, method: :delete, :class => 'link-to-right'  %>
          <a href = "/users/edit">  <img src='/insta-profile.png' class='link-to-right'></a>
        <% else %>
          <%= link_to "Sign Up", new_user_registration_path, :class => 'link-to-right'  %>
          <%= link_to "Log In", new_user_session_path, :class => 'link-to-right'  %>
        <% end %>
  </nav>
        <% if notice %>
          <p class="alert alert-success"><%= notice %></p>
        <% end %>
        <% if alert %>
          <p class="alert alert-danger"><%= alert %></p>
        <% end %>
      </div>
    </ul>
