<header>
  <nav>
    <!-- add help path? -->
    <% if logged_in? %>
      <%= link_to "Log out", logout_path, method: "delete" %>
      <%= link_to 'Add Picture', new_picture_path %>
      <%= link_to "Profile", current_user %>
      <!-- add settings path? -->
    <% else %>
      <%= link_to "Log in", login_path %>
    <% end %>
    <%= link_to "Explore", pictures_path %>
  <nav>
</header>
