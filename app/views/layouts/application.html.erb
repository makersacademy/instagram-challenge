<!DOCTYPE html>
<html>
  <head>
    <title>Joshagram</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

  <nav class="navbar navbar-default">
    <div class= "navbar-container">
      <div class ="navbar-header">
        <button class="navbar-toggle collapsed" data-target="#bs-navbar-collapse-1" data-toggle="collapse" type="button">
          <span class="sr-only">Toggle Navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <div class ="navbar-brand">
          <%= link_to "Joshagram", root_path %>
        </div>
      </div>
    <div class="collapse navbar-collapse" id="bs-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <li>
          <%= link_to content_tag(:i, '', :class => "fa fa-camera-retro fa-lg"), new_post_path %>
        </li>
        <li>
          <%= link_to "Login", '#' %>
        </li>
        <li>
          <%= link_to "Register", '#' %>
        </li>
      </ul>
    </div>
      </div>
  </nav>

  <!-- Standard navigation all over the page enabled by bootstrap navigation (copy and paste job) -->
  <!-- and then changed the list so that it links to parts of the page -->

  <div class="container">
    <%= yield %>
  </div>
  </body>
</html>

<!-- the entire app is now wrapped in a bootstrap class "container" which gives the whole application these features
The container class gives a fixed width determined by screen size -->

