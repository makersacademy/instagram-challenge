- if @pictures.any?
  - @pictures.each do |picture|
    %h2= link_to picture.name, picture_path(picture)
    %h2= picture.description
    %h4= link_to "Edit #{picture.name}", edit_picture_path(picture)
    %h4= link_to "Delete #{picture.name}", picture_path(picture), method: :delete
- else
  %p No pictures yet

%a(href=new_picture_path) Add a picture
