- if @pictures.any?
  - @pictures.each do |picture|
    -# = image_tag picture.image.url(:original)
    =link_to image_tag(picture.image.url(:thumb)), picture_path(picture)
    %br= picture.description
    %br
- else
  %h5 No pictures yet!

=link_to 'Add picture', new_picture_path
