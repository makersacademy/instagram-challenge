- if @pictures.any?
  - @pictures.each do |picture|
    =image_tag picture.image.url(:thumb)
    %h2= picture.caption
    %h2= picture.user
- else
  No pictures yet!

%a(href='/pictures/new') Add a picture
