- if Picture.all.any?
  - @pictures.each do |picture|
    %p= image_tag picture.image.url(:medium), :id => "picture"
    %p= picture.description
    = link_to "Edit", edit_picture_path(picture)
- else
  No pictures here 😳

%a(href='/pictures/new') Add a picture
