- if @pictures.any?
  - @pictures.each do |picture|
    =image_tag picture.image.url(:thumb)
    %h2= picture.caption
    %h2= picture.user
    %h2= link_to "Comment", new_picture_comment_path
- else
  No pictures yet!

%a(href='/pictures/new') Add a picture
