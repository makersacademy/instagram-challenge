<center><h1><%=@picture.title%></h1>
  <h4><%= link_to 'Edit title', edit_picture_path(@picture) %><h4>

    <%=image_tag("photos/#{@picture.filename}")%>
    <h4><%= link_to 'Delete post', picture_path(@picture), method: :delete %><h4>

      <h4><%= link_to 'Add comment', new_picture_comment_path(@picture) %><h4>

        <p> Comments: </p>
        <% @picture.comments.all.each do |comment|%>
        <h4><%=comment.thoughts%></h4>
        <%end%>

        <a href = '/pictures' >Back to  homepage</a></center>
