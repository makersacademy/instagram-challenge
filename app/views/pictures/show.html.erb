<p>
  <strong>Title:</strong>
  <%= @picture.title %>
</p>

<p>
  <% if @picture.image.attached? %>
    <image src="<%= (url_for(@picture.image)) %>"
  <% end %>
</p>

<p>
  <strong>Caption:</strong>
  <%= @picture.caption %>
</p>

<%= link_to 'Back', pictures_path %>
