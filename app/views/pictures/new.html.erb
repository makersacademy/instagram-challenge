<%= form_for(@picture, multipart: true) do |f| %>
  <div class="field">
    <%= f.label :image %>
    <%= f.file_field :image %>
  </div>
  <div class="actions">
    <%= f.submit 'upload pic' %>
    <%= link_to 'Nevermind', pictures_path, class: 'button' %>
  </div>
<% end %>
