<% if @pictures.any? %>
  <% @pictures.each do |picture| %>
    <%= link_to (image_tag picture.image.url(:thumb), alt: picture.caption), picture_path(picture) %>
    <%= picture.caption %>
  <% end %>
<% else %>
  No pictures yet
<% end %>
<%= link_to 'Post a picture', new_picture_path %>