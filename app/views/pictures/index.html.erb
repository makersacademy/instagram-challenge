<a href='pictures/new'>add a picture!</a>
<%  if @pictures.any? %>
  <% @pictures.each do |picture| %>
    <%= link_to( image_tag(picture.image.url(:medium)), picture_path(picture), :class=>'picture', :id=>'1') %>
    <%= link_to '😻', picture_likes_path(picture), class: 'likes-link' %>
    <span class="likes_count"> <%= picture.likes.count %> </span> <%= 'like'.pluralize(picture.likes.count) %>
    <p> <%= picture.caption %> </h2>
      <br>
  <% end %>
    <% else %>
      No pictures yet
    <% end %>
