<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <title>Pictures index</title>
</head>

<body>
  <% if @pictures.any? %>
    <% @pictures.each do |picture| %>
      <%= image_tag picture.picture.url(:thumb), class: "uploaded-pic" %>
      <%= link_to picture.description, picture_path(picture) %>
    <% end %>
    <% else %>
      No pictures yet
    <% end %>
</body>

</html>
