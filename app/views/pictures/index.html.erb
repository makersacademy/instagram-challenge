<% if @pictures.any? %>
  <% @pictures.each do |picture| %>
    <img src="<%=picture.filepath%>" />
    <p><%= picture.description%></p>
    <% end %>
<% else %>
  <h2>No pictures yet</h2>
<% end %>

<h3><a href="#">Add a picture</a></h3>
