<%  if @pictures.any? %>
  <% @pictures.each do |picture| %>
    <%= image_tag picture.image.url(:medium) %>
    <p> <%= picture.caption %> </h2>
      <br>
  <% end %>
    <% else %>
      No pictures yet
    <% end %>

<a href='pictures/new'>Add a picture!</a>
