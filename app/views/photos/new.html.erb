<%= form_with scope: :photo, url: photos_path, local: true do |form| %>

  <%= form.label :image %>
  <%= form.file_field :image%>

  <%= form.label :caption %>
  <%= form.text_area :caption %>

  <%= form.submit "Submit" %>

<% end %>
