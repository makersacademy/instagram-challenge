<%= form_for @photo, html: {multipart: true} do |f| %>
  <%= f.label :description, class: "form label" %>
  <%= f.text_field :description, class: "form" %>
  <%= f.file_field :image, class: "form" %>
  <%= f.submit "Upload", class: "form" %>
<% end %>
