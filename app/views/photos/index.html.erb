<% if @photos.any? %>
  <% @photos.each do |photo| %>
    <%= image_tag photo.image.url(:medium) %>
    <%= photo.description %>
    <%= button_to 'Remove', photo_path(photo), method: :delete %>
  <% end %>
<% else %>
  No photos to show
<% end %>
<%= link_to "Post new pic", new_photo_path %>