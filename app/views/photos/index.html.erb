<% if @photos.any? %>
  <%= form_for @photos, html: {multipart: true} do |photo| %>
    <%= f.file_field :image %>
  <% end %>
<% else %>
No photos yet
<% end %>

<%= link_to "Add a photo", new_photo_path %>
