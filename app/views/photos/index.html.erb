<% if @photos.any? %>
  <% @photos.each do |restaurant| %>
    <span><%= image_tag photo.image.url(:thumb) %></span>
  <% end %>
<% else %>
  <h1>No photos yet</h1>
<% end %>

<a href="#">Add a photo</a>
