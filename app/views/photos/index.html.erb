<h1>FTWinstagram!</h1>

<h2>
  <% if @photos.any? %>
    <% @photos.all.each do |photo| %>
      <%= image_tag photo.image.url, {:width => "500", :height => "500"} %><p>
      <%= photo.caption %><p>
    <% end %>
  <% else %>
    no photos!
  <% end %>

  <form action="/photos/new">
    <input type="submit" value="Upload your photo">
  </form>
</h2>