<%= link_to "Add a photo", new_photo_path %>

<% if @photos.any? %>

  <% @photos.each do |photo| %>
    <%= link_to photo.title, photo_path(photo) %>
    <%= photo.description %>
  <% end %>
<% else %>
  No photos yet
<% end %>
