<% if @photos.all.any? %>
  <% @photos.all.each do |photo| %>
    <h2><%= photo.caption %></h2>
  <% end %>
<% else %>
  <h1>No photos yet</h1>
<% end %>

<%= link_to 'Post a photo', new_photo_path() %>

