<section class="photo-grid">
  <% @photos.each do |photo| %>
    <section class="single-photo"><%= link_to photo_path(photo) do %>
      <%= image_tag photo.image.url %>
    <% end %>
    </section>
  <% end %>
</section>
