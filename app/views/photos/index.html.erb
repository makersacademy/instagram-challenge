<% if @photos.any? %>
	<% @photos.each do |photo| %>
		<%= link_to image_tag(photo.image.url(:medium)), photo_path(photo) %>
		<p><%= photo.caption %></p>
	<% end %>
<% else %>
	<h2>No photos yet</h2>
<% end %>
