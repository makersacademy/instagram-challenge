
  <p id="notice"><%= notice %></p>

   <span class="post_options">
    <%= link_to 'Back', photos_path, class: "btn_style" %> 
    <% if can? :edit, @photo %>
     | <%= link_to 'Edit', edit_photo_path(@photo), class: "btn_style" %> |
      <%= link_to 'Delete',  @photo, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn_style" %>
    <% end %>
  </span>
<div class="Post"> 
    <%= image_tag @photo.photo, class: "image" %>
  <p>
    <%= @photo.description %>
  </p>

 
</div>