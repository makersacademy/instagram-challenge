<%= form_for @photo, html: {multipart: true} do |f| %>
  <%= f.file_field :image %>
  <br>
  <%= f.label :description %>
  <%= f.text_field :description %>
  <br>
  <%= f.submit  "Update photo"%>
<% end %>
