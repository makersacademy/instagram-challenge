<%= form_for @photo do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :description %>
  <%= f.text_field :description %>
  <%= link_to "Delete Photo", photos_path(@photo), method: :delete %>
  <%= f.submit "Edit Photo"%>
<% end %>