<%= form_for @photo, html: {multipart: true} do |photo| %>
  <%= photo.file_field :image %>
  <%= photo.label :caption %>
  <%= photo.text_field :caption %>
  <%= photo.submit 'Update Caption' %>
<% end %>