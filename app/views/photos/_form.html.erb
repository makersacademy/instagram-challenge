  <%= form_for photo, html: {multipart: true} do |f| %>
  <%= f.label :image %><br />
  <%= f.file_field :image %>
  <%= f.label :caption %><br />
  <%= f.text_field :caption %>
  <%= f.submit "#{button_message}" %>
  <% end %>
