<% if user_signed_in? && current_user.id == photo.user_id %>
<%= button_to "Censor photo", edit_photo_path(photo) %>
<%= button_to "Destroy the evidence", photo_path(photo), method: :delete %>
<% end %>
