<h1>Contribute to beauty...or anything really.</h1>


<%= form_for @image, html: {multipart: true} do |f| %>
  
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.file_field :image %>
  <%= f.submit %>
  
  <% end %>
