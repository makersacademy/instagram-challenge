<%= form_for Image.new, html: {multipart: true} do |f| %>
  <%= f.label :description %>
  <%= f.text_field :description %>
  <%= f.label :picture %>
  <%= f.file_field :picture %>
  <%= f.submit %>
<% end %>
