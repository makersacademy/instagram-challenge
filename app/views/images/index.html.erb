
<% if @images.any? %>
  <ul>
    <% @images.each do |image| %>
      <li> <%= image_tag image.image.url(:thumb) %>
           <%= image.name %> <br> <%= image.description %> </li>
    <%end%>  
  </ul>

<% else %>

No images added yet

<%end%>
