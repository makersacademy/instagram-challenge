<p><%= image_tag @image.picture.url(:medium) %><p>
<p><%= @image.name %></p>
   <h3>Comments</h3>
      <% if @image.comments.any? %>
        <ul>
          <% @image.comments.each do |comment| %>
            <li>
              <%= comment.comment %>
              <%= link_to "Delete comment", image_comment_path(@image, comment), method: :delete %>
            </li>
          <% end %>
        </ul>
      <% else %>
        <p>no comments yet</p>
      <% end %>