<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title><%= @image.description %></title>
  </head>
  <body>

    <%= image_tag @image.image.url(:medium) %>
    <%= @image.description %>

  </body>
</html>
