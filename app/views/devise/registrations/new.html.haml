.row.justify-content-center
  .col-12.col-md-8
    %h2.text-center.mb-5 Sign up
    = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
      = devise_error_messages!
      .field.form-group.row
        = f.label :email, class: 'col-4'
        = f.email_field :email, autofocus: true, class: 'col-8 form-control'
      .field.form-group.row
        = f.label :user_name, class: 'col-4'
        = f.text_field :user_name, class: 'col-8 form-control'
      .field.form-group.row
        = f.label :password, class: 'col-4'
        = f.password_field :password, autocomplete: "off", class: 'col-8 form-control'
        - if @minimum_password_length
          %small.form-text.text-muted.col-12= "(#{@minimum_password_length} characters minimum)"
      .field.form-group.row
        = f.label :password_confirmation, class: 'col-4'
        = f.password_field :password_confirmation, autocomplete: "off", class: 'col-8 form-control'
      .form-group.row.justify-content-center.py-4
        = f.submit 'Sign up', class: 'btn-lg btn-primary'
    = render "devise/shared/links"
