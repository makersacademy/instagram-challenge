<% if @scenes.any? %>
  <% @scenes.each do |scene| %>
    <%= link_to scene.title, scene_path(scene) %>
    <%= link_to "Edit #{scene.title}", edit_scene_path(scene) %>
  <% end %>
<% else %>
  <h1>No scenes yet</h1>
<% end %>

<!-- <a href="#">Add a scene</a> -->

<%= link_to 'Add a scene', new_scene_path %>
