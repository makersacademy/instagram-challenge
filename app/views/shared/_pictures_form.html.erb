<%= form_for(@post) do |f| %>
  <div class="field">
    <%= f.text_area :link, placeholder: "post title" %>
    <%= f.file_field :avatar %>
    <%= f.hidden_field :avatar_cache %>
  </div>
  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>