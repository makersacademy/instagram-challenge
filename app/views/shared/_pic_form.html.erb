
<%= simple_form_for @pic do |f| %>
  <%= f.input :image, as: :text%>
  <%= f.input :title, as: :text%>
  <%= f.input :description, as: :text%>
  <br><br>
  <%= f.button :submit, 'Submit' %>
<% end %>
