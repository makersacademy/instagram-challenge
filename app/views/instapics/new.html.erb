<%= form_for @instapic, html: {multipart: true} do |pic| %>
  <%= pic.label :image %>
  <%= pic.file_field :image %><br />
  <%= pic.label :description %>
  <%= pic.text_area :description %><br />
  <%= pic.submit "Share now" %>
<% end %>
