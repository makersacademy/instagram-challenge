
<% if @instapics.all.any? %>
  <% @instapics.all.each do |pic| %>
    <%= image_tag pic.image.url(:medium) %>
    <h4><%= pic.description %></h4>
  <% end %>
<% else %>
  <h2> No pics here! Why don't you share something?</h2>
<% end %>
