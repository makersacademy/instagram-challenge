<% if user_signed_in? %>
  <div class="upload">
    <%= render 'posts/form' %>
  </div>
  <div class="posts">
    <% @posts.each do |post| %>
      <%= render partial: 'posts/post', locals: { post: post } %>
    <% end %>
  </div>
<% end %>
