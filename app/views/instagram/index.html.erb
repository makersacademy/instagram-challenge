<h3>
<% if current_user && current_user.sign_in_count == 1 %>
  Welcome to Instagram <%= current_user.email %>
<% elsif current_user && current_user.sign_in_count > 1 %>
  Welcome back <%= current_user.email %>
<% else %>
  Please sign in
<% end %>
</h3>
<br>
<% if Picture.count == 0 %>
  <h5>  No photos have been added! </h5>
<% end %>

<% @pictures.each do |picture| %>
  <%= image_tag picture.image.url(:medium) %>
  <br>
  <%= picture.description %>
  <br>
  <% if current_user && current_user.id == picture.user_id %>
    <%= link_to "Delete #{picture.description}", picture_path(picture), method: :delete %>
  <% end %>
  <% if Comment.count != 0 %>
    <% these_comments = Comment.where(picture_id: picture.id) %>
    <% if these_comments %>
      <% these_comments.each do |comment| %>
        <%= comment.description %>
      <% end %>
    <% end %>
  <% end %>
  <% if current_user %>
    <%= link_to 'Comment', new_picture_comment_path(picture) %>
  <% end %>
<% end %>
<% if current_user %>
  <%= link_to 'Add a picture', new_picture_path %>
<% end %>