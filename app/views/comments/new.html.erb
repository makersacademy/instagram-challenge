<%= form_for [@post, @comment] do |f| %>
  <%= f.label :message %>
  <%= f.text_area :message %>
  <%= f.label :reaction %>
  <%= f.select :reaction, options_for_select(['omg', 'lol', 'yay', 'nope']) %>
  <%= f.submit 'Add Comment' %>
<% end %>
