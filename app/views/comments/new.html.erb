<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Leave a comment</title>
  </head>
  <body>
    <%= form_for [@picture, @comment] do |f| %>
      <%= f.label :comment %>
      <%= f.text_area :comment %>
      <%= f.submit 'Post' %>
    <% end %>
  </body>
</html>
