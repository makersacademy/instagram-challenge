<h3>Comments for <%= post.name %></h3>

<% if post.comments.any? %>
  <ul>
    <% post.comments.each do |comment| %>
      <li>
        <%= comment.message %>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>No comments.</p>
<% end %>
