<%= form_for [:comment, commentable], method: 'post', remote: true do |f| %>
  <%= f.fields_for commentable.comments.new do |ff| %>
    <%= ff.text_area :message %>
  <% end %>
  <%= f.submit "Comment" %>
<% end %>
