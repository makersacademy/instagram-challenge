
  <strong><%= comment.user.name %></strong> ·
  <%= comment.body %> <%= link_to '╳', [comment.picture, comment],
              method: :delete,
              data: {confirm: "Are you sure?" } %><br>
  <%= distance_of_time_in_words_to_now(comment.created_at) %> ago<br><br>
