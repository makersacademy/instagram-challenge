import React from "react";
import Routes from "../routes/Index";

export default props => <>{Routes}</>;
// Routes are rendered at the entry of the app, so routes available when the application is loaded.