<%= form_for @photo, html: { multipart: true } do |f| %>
  <%= f.file_field :insta_photo %>
  <%= f.hidden_field :insta_photo_cache %>
  <%= f.label :caption %>
  <%= f.text_area :caption %>
  <%= f.submit %>
<% end %>
