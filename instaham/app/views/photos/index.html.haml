-if @photos.empty?
  %p No ham makes us cry! Click here to add the first photo:
-else
-if current_user
  = link_to "Add Photo", new_photo_path
%br
- @photos.each do |photo|
  %br
  = photo.message
  - photo.comments.each do |comment|
    = comment.text
  - if current_user
    = link_to "Comment on this photo", new_photo_comment_path(photo)