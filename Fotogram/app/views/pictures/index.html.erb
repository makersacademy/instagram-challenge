<h1>FOTOGRAM</h1>
<% @pictures.each do |picture|%>

  <h2><%= link_to picture.title, picture %></h2>
  <li><%= link_to image_tag(picture.image.url(:medium)), picture %></li>

<% end %>

<h3><%= link_to "New Post", new_picture_path %></h3>
