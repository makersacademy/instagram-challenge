<h1>FOTOGRAM</h1>
<% @pictures.each do |picture|%>

  <li><%= link_to image_tag(picture.image.url(:medium)), picture %></li>
   <%= link_to picture.title, picture %>
 
<% end %>

<%= link_to "New Post", new_picture_path %>
