<% if Photo.all.any? %>
  <% Photo.all.each do |photo| %>
    <h3 class='image'><%= image_tag photo.image.url(:thumb), {:width => "500", :height => "500"}%></h3>
    <h4><%= photo.name %></h4>
    <p><%= photo.description %></p>
  <% end %>
<% else %>
  <h1>No photos yet!</h1>
<% end %>

<h3><%= link_to 'Add a photo', new_photo_path %></h3>