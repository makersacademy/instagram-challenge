<% if Photo.all.any? %>
  <% Photo.all.each do |photo| %>
    <h2><%= photo.name %></h2>
    <h3><%= photo.description %></h3>
  <% end %>
<% else %>
  <h1>No photos yet!</h1>
<% end %>

<h3><%= link_to 'Add a photo', new_photo_path %></h3>