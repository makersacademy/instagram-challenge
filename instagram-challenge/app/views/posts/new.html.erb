Add Post

<%= form_for Post.new, html: {multipart: true} do |field| %>
	<%= field.label :caption, "Caption" %>
	<%= field.text_field :caption %>
	<%= field.label :image, "Image" %>
	<%= field.file_field :image %>
	<%= field.submit "Post" %>
<% end %>