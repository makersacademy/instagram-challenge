<% if Post.all.any? %>
	<% Post.all.each do |post| %>
		<%= post.caption %>
		<%= image_tag post.image.url(:thumb) %>
	<% end %>
<% else %>
	No posts yet
<% end %>

<%= link_to "New Post", new_post_path %>