<%= form_with(model: like, local: true) do |form| %>
  <% if like.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(like.errors.count, "error") %> prohibited this like from being saved:</h2>

      <ul>
      <% like.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :number_of_likes %>
    <%= form.number_field :number_of_likes, id: :like_number_of_likes %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :like_user_id %>
  </div>

  <div class="field">
    <%= form.label :picture_id %>
    <%= form.text_field :picture_id, id: :like_picture_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
