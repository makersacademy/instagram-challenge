<%= form_for @image, html: {multipart: true} do |f| %>

  <%= f.label :picture %>
  <%= f.file_field :picture %>
  <%= f.label :description %>
  <%= f.text_field :name %>
  <%= f.submit 'Add photo' %>

<% end %>