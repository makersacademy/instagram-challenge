<% if @photos.empty? %>
  <%= 'No photos to show :(' %>
<% else %>
  <% @photos.each do |photo| %>
    <%= photo.location %> <br>
    <%= photo.image %> <br>
    <%= photo.description %> <br>
  <% end %>
<% end %>


<%= link_to 'Post a photo', new_photo_path %>
