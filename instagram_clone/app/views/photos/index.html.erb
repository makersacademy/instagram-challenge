<% if @photos.any? %>
  <% @photos.reverse.each do |p| %>
    <%= link_to image_tag(p.image.url(:medium), id: p.id),photo_path(p) %>
    <p><%= p.caption %></p>
  <% end %>
<% else %>
  No photos yet :(
<% end %>
<%= link_to 'Upload Photo', new_photo_path %>
