Post by <%= @post.user.email %>
<br />
<em>"<%= @post.caption %>". . .</em>
<br />
<%= image_tag(@post.image.url(:medium), height: "400",:class => "img-rounded") %>
<br />
<% @post.comments.each do |comment| %>
  <%= comment.thoughts %>
  <br />
<% end %>
