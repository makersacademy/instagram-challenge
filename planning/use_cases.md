# User Stories

```
Non-logged in users can view images and tags and comments but cannot create any of them

Images have their own page showing its tags, comments and likes

Tags have their own page showing all photos with that tag

Authenticated users can add images

They can delete images from their image roll but no others

They can add tags to all images

They can add comments to all images

```
