

en:
  hello: "Hello world"
  activerecord:
    errors:
      models:
        pict:
          attributes:
            image:
              blank: "Error, you need to add an image"
      full_messages:
        format: "%{message}"
