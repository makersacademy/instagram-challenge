

en:
  hello: "Hello world"
  activerecord:
    errors:
      models:
        pict:
          attributes:
             image:
              blank: "You need to add an image"
             title:
              blank: "You need a title! (Don't forget a question mark)"
        user:
          attributes:
            email:
              taken: 'Account with email already exists, why not try logging in instead?'
            user_name:
              taken: 'Username already taken'
      full_messages:
        format: "%{message}"
