- if @images.any?
  - @images.each do |image|
    %h2= link_to image.name, image_path(image)
    = image_tag image.image.url(:thumb)
    = image.created_at
- else
  No images yet
%a(href='/images/new') Add a image
