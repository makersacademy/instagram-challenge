<%= form_for @picture, :url => pictures_path, :html => { :multipart => true } do |picture| %>

  <%= picture.label :name, "Name" %>
  <%= picture.text_field :name %>
  <%= picture.label :description, "Description" %>
  <%= picture.text_field :description %>
  <%= picture.label :image, "Image" %>
  <%= picture.file_field :image %>
  <%= picture.submit "Upload picture" %>

<% end %>
