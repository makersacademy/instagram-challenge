<% if @pictures.any? %>
  <% @pictures.reverse.each do |picture| %>
    <h2> <%= picture.caption %> </h2>
    <p> <%= image_tag picture.image.url(:medium) %> </p>
    <% end %>
<% else %>
"No pictures yet"
<% end %>
